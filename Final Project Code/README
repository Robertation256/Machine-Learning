1. RawData is what we got from shixiseng after filtering out invalid entries
2. data_preprocessing file processes on RawData and genrates TestData.csv and TrainData.csv
3. NN is a hand-built neural network using Pytorch, while MLP is just utilizing sklearn MLP package
